


<%@teams.each do |w|%>
    <div id="pole">
    <div id="pole"> <p style="margin: 10px; text-align: center;  "><%= link_to w.name, {id: w.id, action: 'team' } %> <br></p>

    </div>

    <div id="pole">
      <%=link_to (image_tag w.avatar.url(:banner)),{id: w.id, action: 'team' }%>

         </div>
    </div>

<%end%>